[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
django = ">=4.2.0,<5.0"
djangorestframework = ">=3.14.0"
django-cors-headers = ">=4.0.0"
python-decouple = ">=3.8"
psycopg2-binary = ">=2.9.0"

[dev-packages]
pytest = ">=7.0.0"
pytest-django = ">=4.5.0"
black = ">=23.0.0"
flake8 = ">=6.0.0"
isort = ">=5.12.0"

[requires]
python_version = "3.11"
