export const FreeAllowedMessageCount = 4;

export enum Theme {
  Light = "light",
  Dark = "dark",
  System = "system",
}

export const AsyncStorageKey = {
  AppTheme: "@appTheme",
  Token: "@token",
};
